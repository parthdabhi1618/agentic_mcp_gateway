import React from 'react'; export default function HistorySidebar({ history, onRestore}){if(!history || history.length=== 0){return(<div className="glass-panel" style={{ padding: 24, borderRadius: 12}}><h3 style={{ fontSize: 18, color: 'var(--primary)', marginBottom: 8}}> Past Executions </h3><p style={{ color: 'var(--outline-variant)', fontSize: 14}}> No history yet. Runs will appear here. </p></div>);}return(<div className="glass-panel" style={{ padding: 24, borderRadius: 12}}><h3 style={{ fontSize: 18, color: 'var(--primary)', marginBottom: 16}}> Past Executions </h3><div style={{ display: 'flex', flexDirection: 'column', gap: 12}}>{history.map((run, index)=>{const hasError=run.logs?.some((l)=> l.status=== 'failed'); const timeString=new Date(run.timestamp).toLocaleTimeString([],{hour: '2-digit', minute: '2-digit',}); return(<div key={index} onClick={()=> onRestore(run)} style={{ background: 'var(--surface-container-low)', padding: '12px 16px', borderRadius: 8, cursor: 'pointer', borderLeft: `4px solid ${hasError?'var(--error)':'var(--tertiary)'}`, transition: 'background 0.2s ease',}} onMouseEnter={(e)=>{e.currentTarget.style.background='var(--surface-container-high)';}} onMouseLeave={(e)=>{e.currentTarget.style.background='var(--surface-container-low)';}} ><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 4,}} ><span style={{ fontSize: 12, color: 'var(--outline-variant)'}}>{timeString} </span>{hasError &&(<span style={{ fontSize: 10, color: 'var(--on-error-container)', background: 'var(--error-container)', padding: '2px 6px', borderRadius: 4, fontWeight: 'bold',}} > FAILED </span>)} </div><div style={{ fontSize: 14, color: 'var(--on-surface)', whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis',}} > "{run.prompt}" </div><div style={{ fontSize: 12, color: 'var(--outline-variant)', marginTop: 4}}>{run.steps?.length || 0} steps </div></div>);})} </div></div>);}
